# Public URL only. Never commit access tokens or API keys.
# Physical device: use the LAN IP of the computer running `npm run api`.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:8787/api
EXPO_PUBLIC_APP_ENV=mock
